<div class="col-lg-4">
    <a class="card-link" href="#">
        <div class="card">
            <img src="{{ asset(country_image) }}" class="card-img-top" alt="..." style="height:193px;">
            <div class="card-body">
                <a class="card-title" href="{{ path('offer_browse', {'destinationName': country_name}) }}">{{ country_name }}</a>
            </div>
        </div>
    </a>
</div>